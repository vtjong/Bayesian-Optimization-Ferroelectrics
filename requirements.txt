scipy
numpy
matplotlib
pandas
scikit-learn
# pandas Excel backend: run_calibration reads the archival PUND/joulemeter workbooks
openpyxl

# Optional crystal-structure visualization extras (pymatgen, mp-api, ase) live in
# requirements-viz.txt - install separately: pip install -r requirements-viz.txt
# Test-only dependencies live in requirements-dev.txt.
